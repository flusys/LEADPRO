import { ExpenseTypeLabelPipePipe } from './expense-type-label-pipe-pipe';

describe('ExpenseTypeLabelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExpenseTypeLabelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
